import React from 'react';
import ReactDOM from 'react-dom';
import { CreateAccount } from './createaccount';

ReactDOM.render(
    <div>
        <CreateAccount/>
    </div>, document.getElementById("createAccount")
);
